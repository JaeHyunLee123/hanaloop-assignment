# 구현 계획: 더미 데이터 생성 방식 고도화

## 목표
`fake-db.ts`에서 수동으로 난수를 생성하던 방식을 `api.ts`의 `submitEmissions` 핵심 로직을 공유하여 사용하도록 개선합니다. 이를 통해 더미 데이터가 실제 서비스 로직과 일관성을 갖도록 합니다.

## 주요 변경 사항
1.  **로직 분리**: `api.ts`에 있던 배출량 계산 및 상태 업데이트 로직을 `src/lib/emission-service.ts`로 분리합니다. 이 함수는 동기적으로 작동하며 부수 효과(지연, 실패)가 없습니다.
2.  **`api.ts` 업데이트**: 분리된 `applyEmissions`를 `submitEmissions` 내부에서 호출하도록 수정합니다.
3.  **`fake-db.ts` 업데이트**:
    *   기존의 `generateKenderEmissions` 등 개별 회사별 생성 함수를 제거합니다.
    *   `applyEmissions`를 사용하여 매월 생산, 운송, 수입 액션을 시뮬레이션하여 데이터를 생성합니다.
4.  **테스트 및 검증**: `npm run test`를 실행하여 데이터가 올바르게 생성되고 필터링되는지 확인합니다.

## 제약 사항
- 테스트 실패 시 최대 2회까지만 수정 시도.
- 순환 참조(Circular Dependency) 방지.
- 2025-01부터 현재 전월까지의 기간 유지.
- 랜덤 UUID 사용.
